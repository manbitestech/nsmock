# Release Notes

Changes since the `v0.0.1` tag.

## Package renamed to `@manbitestech/nsmock` - namespacing needed.


## Breaking change: `Record.initType` → `Record._initType`

## Documentation
- Update README with **Supported Modules** and **Test Utilities**
- New cleaner import style for `Record` module.


## Tests
- Updated `__test__/record-nextid.test.js` to use `Record._initType` throughout.

